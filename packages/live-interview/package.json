{
  "name": "@auditforge/live-interview",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "AuditForge ISO 42001 — Live interview composer: session lifecycle, consent gating, transcript-to-clause routing, audit-ledger emission. v3 Phase 7.6.",
  "license": "BUSL-1.1",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts",
    "./session": "./src/session.ts",
    "./composer": "./src/composer.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src tests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@auditforge/diarization": "workspace:*",
    "@auditforge/transcription": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "@vitest/coverage-v8": "^2.1.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
