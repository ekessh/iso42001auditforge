{
  "name": "@auditforge/mcp-tools",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Shared typed MCP tool descriptors. Zod schemas + @modelcontextprotocol/sdk-compatible Tool definitions consumed by apps/mcp-server and any future internal MCP clients.",
  "license": "BUSL-1.1",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src tests"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
