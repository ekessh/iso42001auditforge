{
  "name": "@auditforge/interview-library",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Curated catalogue of ISO 42001 interview question sets per role / clause area / AI system class. Provides deterministic composer that fits a time-boxed plan, plus indexable surface for hybrid search. Additive to @auditforge/conversational-engine question-library — feeds live-interview mode without replacing it.",
  "license": "BUSL-1.1",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./domain": "./src/domain/index.ts",
    "./loader": "./src/loader/index.ts",
    "./composer": "./src/composer/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src tests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@auditforge/shared": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.2",
    "vitest": "^4.1.0"
  }
}
