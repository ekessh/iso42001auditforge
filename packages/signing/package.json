{
  "name": "@auditforge/signing",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Ed25519 detached-signature service with JCS (RFC 8785) canonicalization. Software key provider plus pluggable PKCS#11 / KMS interface.",
  "license": "BUSL-1.1",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./jcs": "./src/jcs.ts",
    "./signer": "./src/signer.ts",
    "./providers": "./src/providers/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint src tests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@auditforge/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "fast-check": "^3.22.0",
    "typescript": "^5.6.2",
    "vitest": "^4.1.6"
  }
}
