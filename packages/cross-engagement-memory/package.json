{
  "name": "@auditforge/cross-engagement-memory",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "AuditForge Phase 15 — anonymized per-firm cross-engagement pattern memory. Read-only; no auditee identifiers, no finding text. BUSL-1.1.",
  "license": "BUSL-1.1",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./domain": "./src/domain.ts",
    "./aggregator": "./src/aggregator.ts",
    "./query": "./src/query.ts",
    "./anonymize": "./src/anonymize.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src tests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.2",
    "vitest": "^4.1.6"
  }
}
