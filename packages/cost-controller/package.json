{
  "name": "@auditforge/cost-controller",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Per-engagement LLM budget controller with warn-at-80%/hard-stop-at-100% semantics, budget event emission, and Drizzle-backed persistence. BUSL-1.1.",
  "license": "BUSL-1.1",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src tests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@auditforge/shared": "workspace:*",
    "drizzle-orm": "^0.36.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.2",
    "vitest": "^3.2.6"
  }
}
