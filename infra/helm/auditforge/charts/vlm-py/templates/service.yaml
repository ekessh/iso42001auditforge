{{/* SPDX-License-Identifier: BUSL-1.1 */}}
{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "vlm-py.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "vlm-py.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vlm-py.selectorLabels" . | nindent 4 }}
{{- end }}
