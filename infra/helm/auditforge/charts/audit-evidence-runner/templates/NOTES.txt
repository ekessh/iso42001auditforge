{{/* SPDX-License-Identifier: BUSL-1.1 */}}
AuditForge audit-evidence-runner is installed.

To verify:
  kubectl -n {{ .Release.Namespace }} get pods -l app.kubernetes.io/component=audit-evidence-runner

Sandbox isolation: runtimeClassName={{ .Values.runtimeClassName | default "(default)" }}.
For prod set runtimeClassName: gvisor or kata-clh.
