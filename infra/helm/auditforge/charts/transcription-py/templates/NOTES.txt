{{/* SPDX-License-Identifier: BUSL-1.1 */}}
AuditForge transcription-py is installed.

To verify:
  kubectl -n {{ .Release.Namespace }} get pods -l app.kubernetes.io/component=transcription-py

Sandbox isolation: runtimeClassName={{ .Values.runtimeClassName | default "(default)" }}.
For prod set runtimeClassName: gvisor or kata-clh.
